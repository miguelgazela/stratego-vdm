/**
  Coord.

  <p>
  description
  </p>

  @version
  @author
*/

class Coord

types

values

instance variables
    public x : nat;
    public y : nat;

    inv x >= 1 and x <= 10;
    --inv y >= 1 and y <= 10; TODO perguntar à prof

operations

-- constructor for Coord
public Coord: nat * nat ==> Coord
Coord(x_, y_) == (
  x := x_;
  y := y_;
  return self
);

public getX: () ==> nat
getX() ==
  return x;

public getY: () ==> nat
getY() ==
  return y;

public setPos: nat * nat ==> ()
setPos(x_, y_) == (
  x := x_;
  y := y_;
)
pre x_ >= 1 and x_ <= 10 and y_ >= 1 and y_ <= 10
post x = x_ and y = y_;

functions

end Coord
